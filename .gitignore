# config.js agora contém apenas chaves PÚBLICAS do Supabase (URL + anon), seguras
# para versionar. A chave da IA (DeepSeek) fica como segredo na Edge Function.

# Arquivos do sistema operacional e IDEs
.DS_Store
Thumbs.db
.vscode/
.idea/
.env
node_modules/
